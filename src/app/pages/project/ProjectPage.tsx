import ProjectContent from '../../../features/project/components/ProjectContent';

// ProjectPage is the route-level wrapper for the project portfolio screen.
// All the real UI lives in ProjectContent — this file just connects the
// router to the feature component.
const ProjectPage = () => <ProjectContent />;

export default ProjectPage;
